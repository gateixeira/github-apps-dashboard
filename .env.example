# Optional: Set these values to avoid entering them in the UI
GITHUB_TOKEN=your_personal_access_token
GITHUB_ENTERPRISE_URL=https://github.example.com/api/v3
PORT=3001

# Number of days to look back in audit logs to determine app activity (default: 90)
INACTIVE_DAYS=90
